<%= form_tag("/nukes", :method => "post", :id => "nuke_form") do %>
<fieldset>
  <%= hidden_field_tag(:game_id, @game.id) %>

  	<%= label_tag(:y_value, "Y:") %>
 	<%= text_field_tag(:y_value) %>

  	<%= label_tag(:x_value, "X:") %>
  	<%= text_field_tag(:x_value) %>
  
  <%= submit_tag("Send the Bomb-ba") %>
</fieldset>
<% end %>